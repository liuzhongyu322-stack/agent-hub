# 长期记忆

本文件只保存跨任务仍然有用的稳定信息。Agent 可以在新对话中读取本文件，但不要由此自动加载任何旧任务。

## 用户偏好

- 默认使用中文沟通。
- 倾向先实现简单可用的版本，再根据实际使用逐步完善。
- 电脑端是主要工作环境，手机端兼容性后续按实际需要测试。

## 工作系统

- GitHub 私有仓库将作为跨 Agent 规则、记忆、任务摘要和 Skill 清单的统一来源。
- 新任务不继承旧任务进度；只有明确续接时才读取任务 checkpoint。
- 暂不购买云服务器，出现高频跨设备或自动检索需求后再评估。

## 维护规则

- 新增内容前确认它是否会在未来多个任务中复用。
- 内容发生变化时直接更新原条目，避免追加互相矛盾的记录。
- 不确定或未经用户确认的信息不得写成长期事实。
