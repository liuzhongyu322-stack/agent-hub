# 通用规则

本文件保存所有 Agent 都应遵守的稳定规则。内容应保持简短；临时任务要求不要写在这里。

## 对话与上下文

- 默认使用中文与用户沟通，代码、命令和专有名词按实际情况保留原文。
- 新对话只读取本文件和 `MEMORY.md`，不要自动读取旧任务记录。
- 只有用户明确要求继续、恢复或交接旧任务时，才读取 `tasks/current.md`。
- 当前对话中的明确要求优先于本文件和历史记忆。
- 不确定信息应明确说明，不要把推测写成事实。

## 工作方式

- 修改前先了解现状，保护用户已有文件和未提交改动。
- 优先完成最小可用方案，再根据真实问题逐步完善。
- 保持改动范围聚焦，避免无关重构和不必要的复杂设计。
- 任务结束前按风险进行验证，并清楚说明结果及未验证部分。
- 切换 Agent 或暂停未完成任务前，更新 `tasks/current.md`。

## 记忆管理

- 只有跨任务仍有价值、稳定且可复用的信息才能写入 `MEMORY.md`。
- 不保存完整原始对话、推理过程、临时缓存或一次性细节。
- 已失效的记忆应删除或更新，不能与当前事实并存而不作说明。

## 安全与隐私

- 不向本仓库写入密码、API Key、访问令牌、Cookie、私钥或恢复码。
- 涉及个人、客户或项目敏感信息时，只保存完成任务所需的脱敏摘要。
- 外部推送、发布、删除或其他难以恢复的操作，应先确认目标和影响。
